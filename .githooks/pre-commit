#!/bin/bash
set -e

python3 scripts/embed_readme.py examples/

# Re-stage any README files updated by the script
while IFS= read -r -d '' f; do
    git add "$f"
done < <(git diff --name-only -z -- 'examples/*/README.md')
